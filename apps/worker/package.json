{
  "name": "@newsroom/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./rank": {
      "types": "./src/rank.ts",
      "default": "./src/rank.ts"
    }
  },
  "dependencies": {
    "@newsroom/ai": "workspace:*",
    "@newsroom/db": "workspace:*",
    "@newsroom/sources": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx --env-file=../../.env watch src/index.ts",
    "start": "pnpm --filter @newsroom/sources build && pnpm --filter @newsroom/ai build && tsx --env-file=../../.env src/index.ts",
    "ingest": "pnpm --filter @newsroom/sources build && pnpm --filter @newsroom/ai build && tsx --env-file=../../.env src/index.ts ingest",
    "rank": "pnpm --filter @newsroom/sources build && pnpm --filter @newsroom/ai build && tsx --env-file=../../.env src/index.ts rank",
    "prune-scores": "tsx --env-file=../../.env src/index.ts prune-scores",
    "test": "tsx --test --test-force-exit src/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
